# گزارش تجربیات فنی: ساخت سیستم‌های AI روی Cloudflare Workers

این گزارش خلاصه‌ی همه‌ی درس‌هایی‌ست که در ساخت پروژه‌های `gemini-hermes-router` (روتر
چندکلید/چندمدل جلوی Gemini) و `gemini-personal-agent` (ایجنت شخصی تلگرامی) به دست
اومد — نه فقط برای این پروژه‌ها، بلکه به‌عنوان یک مرجع عمومی برای هر پروژه‌ی آینده‌ای
که Cloudflare Workers + یک API هوش مصنوعی رو ترکیب می‌کنه.

---

## ۱) مدل اجرایی Cloudflare Workers — نکته‌ی مرکزی کل معماری

### CPU time در برابر Wall-clock time (این دو کاملاً متفاوتن)
- **CPU time**: فقط زمانی که واقعاً کد JS در حال اجراست (پردازش، parse، حلقه). پلن
  رایگان Worker معمولی: **۱۰ میلی‌ثانیه** به‌ازای هر درخواست.
- **Wall-clock time**: کل زمانی که یک درخواست طول می‌کشه، شامل صبر برای شبکه
  (`fetch` به یک API دیگه). **زمان صبر برای شبکه جزو CPU time حساب نمی‌شه.**
- نتیجه: یک Worker که فقط منتظر جواب یک API دیگه‌ست (حتی چند دقیقه) ممکنه کمتر از
  ۱ میلی‌ثانیه CPU واقعی مصرف کنه. محدودیت واقعی، زمان صبر منطقی (timeout) است، نه
  بودجه‌ی CPU.
- **قانون طلایی معماری**: Worker اصلی (fetch handler) رو سبک نگه دار — فقط
  روتینگ، parse، و صدا زدن سرویس‌های دیگه. هر منطق سنگین (کوئری‌های زیاد، حلقه‌های
  پردازشی) رو ببر تو یک **Durable Object**.

### چرا Durable Object؟ (بودجه‌ی CPU جداگانه و بزرگ‌تر)
- Durable Objects (حتی روی پلن رایگان، با backend مبتنی بر SQLite) بودجه‌ی CPU
  جداگانه و خیلی بزرگ‌تری دارن: پیش‌فرض **۳۰ ثانیه**، قابل افزایش تا ۵ دقیقه.
- این یعنی: منطق rate-limiting، جستجوی برداری brute-force، پردازش پیچیده‌ی
  دیتابیس — همه‌ی این‌ها امن‌تره اگه داخل یک DO انجام بشن، نه مستقیم توی fetch
  handler.
- **الگوی عملی**: `wrangler.toml` باید migration با `new_sqlite_classes` داشته
  باشه تا روی پلن رایگان کار کنه (backend قدیمی‌تر DO فقط روی پلن Paid مجازه).

```toml
[[durable_objects.bindings]]
name = "MY_DO"
class_name = "MyDurableObject"

[[migrations]]
tag = "v1"
new_sqlite_classes = ["MyDurableObject"]
```

### `ctx.waitUntil()` — پاسخ سریع، کار پس‌زمینه
برای webhookها (تلگرام، هر وبهوک دیگه)، همیشه فوری `200 OK` برگردون و کار واقعی رو
با `ctx.waitUntil(...)` در پس‌زمینه انجام بده. اگه صبر کنی تا کار تموم بشه، سرویس
فرستنده (مثلاً تلگرام) ممکنه فکر کنه webhook fail شده و دوباره‌ارسال (retry) کنه —
که باعث پردازش تکراری می‌شه.

```js
export default {
  async fetch(request, env, ctx) {
    // ... کار سریع (auth, parse) ...
    ctx.waitUntil(doTheRealWork(request, env)); // در پس‌زمینه
    return new Response("ok"); // فوری برگرد
  }
}
```

⚠️ نکته‌ی تست: در محیط Node.js محلی (نه Workers واقعی)، تایمرهای بدون `ref` (مثل
داخل `AbortSignal.timeout`) ممکنه باعث بشن پروسه زودتر از موعد خارج بشه و تست‌هات
گمراه‌کننده جواب بدن. این فقط مشکل محیط تستِ Node است، نه رفتار واقعی Workers.

### مطالعه‌ی موردی: کدوم بهینه‌سازی واقعاً عدد CPU-time خودِ Worker رو پایین آورد؟

یک درس عملی مستقیم از تمایز بالا: وقتی داشتیم `gemini-hermes-router` رو بهینه می‌کردیم
(CPU time نزدیک سقف ۱۰ میلی‌ثانیه بود)، دو بهینه‌سازی جدا هم‌زمان انجام شد — (۱) کش
کردن ترجمه‌ی schema ابزارها که مستقیم داخل خودِ Worker (`nativeTranslate.js`) اجرا
می‌شه، و (۲) کش کردن state (مدل‌ها/کلیدها/شمارنده‌های ریت‌لیمیت) داخل یک Durable
Object که از طریق `await stub.method()` صدا زده می‌شه. نتیجه‌ی نهایی (CPU time از ~۹
به ~۵ میلی‌ثانیه) خوب بود، ولی سهم دقیق هرکدوم مشخص نبود چون هر دو با هم دیپلوی شدن.

طبق همون تمایز بالا (زمان `await` روی یک فراخوانی DO جزو CPU-time خودِ Worker حساب
نمی‌شه، چون DO یک execution context جدا با بودجه‌ی خودشه)، منطقی‌ترین حدس اینه که سهم
بزرگ‌تر از کش شماره (۱) اومده باشه (چون مستقیم توی همون execution context Worker
اجرا می‌شه)، نه از کش شماره (۲) — که با این حال هنوز کاملاً بی‌فایده نبود، چون سودش رو
در جای دیگه‌ای می‌ده: latency واقعی wall-clock هر درخواست (چون Worker همچنان باید صبر
کنه تا DO جواب بده، حتی اگه اون صبر روی بودجه‌ی CPU خودش حساب نشه)، حاشیه‌ی امن بودجه‌ی
CPU خودِ DO (که با رشد داده می‌تونست یک روز تنگ بشه)، و مقاومت سیستم زیر بار هم‌زمان.

**درس عملی برای آینده**: اگه هدفت مشخصاً کم‌کردن عدد CPU-time خودِ **Worker** (نه DO)
است، دنبال کدی بگرد که **مستقیم داخل fetch handler یا ماژول‌های import-شده در همون
Worker** اجرا می‌شه — نه کدی که فقط از طریق یک DO stub صدا زده می‌شه. برای تأیید دقیق
این تمایز، اندازه‌گیری جدا (مثلاً با `wrangler tail`/Observability، قبل و بعد از هرکدوم
از این دو تغییر به‌تنهایی) لازمه؛ حدس تئوریک کافی نیست ولی نقطه‌ی شروع درستیه.

---

## ۲) Durable Objects — نکات عملی که فقط با تجربه به دست میان

### API ذخیره‌سازی SQLite
```js
const cursor = this.ctx.storage.sql.exec(`SELECT * FROM t WHERE x = ?`, val);
const rows = cursor.toArray(); // آرایه‌ای از آبجکت‌ها
```
این API **سنکرونه** (نه Promise) — برخلاف API قدیمی‌تر KV-style که Promise برمی‌گردوند.

### Migration بدون از دست دادن داده روی یک DO که از قبل deploy شده
`CREATE TABLE IF NOT EXISTS` فقط برای نصب تازه کافیه. برای یک DO که از قبل در حال
اجراست و باید ستون جدید بهش اضافه بشه، باید `ALTER TABLE` رو هم اجرا کنی — ولی این
روی نصب تازه (که ستون از اول با CREATE TABLE اومده) خطای «duplicate column» می‌ده.
راه‌حل: هر ALTER رو در try/catch بذار:

```js
const MIGRATIONS = [
  `ALTER TABLE models ADD COLUMN kind TEXT NOT NULL DEFAULT 'chat'`,
];
for (const stmt of MIGRATIONS) {
  try { this.ctx.storage.sql.exec(stmt); } catch { /* از قبل وجود داره، مشکلی نیست */ }
}
```
این رو **همیشه** با یک دیتابیس شبیه‌سازی‌شده‌ی «قدیمی» تست کن، نه فقط دیتابیس تازه.

### باگ کلاسیک: فراموش کردن `await` روی متدهای stub
هر متد یک Durable Object که از طریق stub صدا زده بشه (`env.MY_DO.get(id)`)، حتی اگه
خودِ متد داخل کلاس `async` نباشه، **از بیرون همیشه یک Promise برمی‌گردونه** (چون از
مرز RPC رد می‌شه). فراموش کردن `await` باعث race condition می‌شه: کد بعدی
(مثلاً خوندن همون داده از DB) ممکنه قبل از تموم شدن نوشتن اجرا بشه.

```js
// ❌ باگ ظریف: نوشتن ممکنه هنوز کامل نشده باشه وقتی خط بعد اجرا میشه
stub.appendMessage({...});
const history = await stub.getHistory();

// ✅ درست
await stub.appendMessage({...});
const history = await stub.getHistory();
```
این باگ رو در محیط تست فقط با شبیه‌سازی «تأخیر واقعی RPC» (پیچیدن متدها در
`setTimeout`) می‌شه معتبر لو داد — تست synchronous ساده این کلاس باگ رو قایم می‌کنه.

### قفل هم‌زمانی (concurrency lock) بدون سرویس جدا
چون یک instance از DO درخواست‌های خودش رو **سریال** پردازش می‌کنه، می‌شه یک قفل ساده
in-memory ساخت (بدون نیاز به Redis/queue جدا) که از تداخل دو عملیات هم‌زمان (مثلاً
دو پیام پشت‌سرهم از یک کاربر) جلوگیری کنه:

```js
acquireLock() {
  const now = Date.now();
  if (this._lockedAt && now - this._lockedAt < STALE_MS) return false;
  this._lockedAt = now;
  return true;
}
releaseLock() { this._lockedAt = null; }
```
همیشه یک «stale timeout» بذار (مثلاً ۱۰-۱۵ دقیقه) تا اگه یک عملیات crash کرد و قفل
رو آزاد نکرد، سیستم برای همیشه قفل نمونه.

### DO Alarms — ابزار درست برای debounce/تأخیر دقیق و برای همگام‌سازی دوره‌ای
اگه نیاز به «صبر کن چند ثانیه ببین چیز دیگه‌ای هم میاد، بعد یک‌جا پردازش کن» داری
(مثلاً چند فایل که با هم می‌رسن)، **از Cron Trigger استفاده نکن** (خیلی درشت‌دونه‌ست،
حداقل هر ۱ دقیقه). به‌جاش از Alarm خودِ DO استفاده کن:

```js
async bufferItem(item) {
  // ... ذخیره‌ی item ...
  await this.ctx.storage.setAlarm(Date.now() + 1500); // ۱.۵ ثانیه دیگه
}
async alarm() {
  // پردازش همه‌ی چیزهایی که تا الان جمع شدن
}
```
هر DO فقط یک alarm در هر لحظه می‌تونه داشته باشه — اگه چند batch مستقل هم‌زمان در
حال جمع‌شدنن، باید منطق «کدوم زودتر باید پردازش بشه» رو خودت مدیریت کنی (پیدا کردن
min زمان، و بعد از هر alarm، دوباره‌زمان‌بندی برای باقی‌مونده‌ها).

همین ابزار (نه یک `setInterval` جاوااسکریپت که با evict شدن DO از بین می‌ره) برای
همگام‌سازی دوره‌ای state درون‌حافظه‌ای با SQLite هم درسته — ادامه‌ش رو در الگوی زیر
ببین.

### الگوی کش درون‌حافظه‌ای در یک DO تک‌نویسنده (write-through + batch-flush)

یک الگوی کامل و تست‌شده برای وقتی که یک متد پرتکرار (اینجا `pickCandidate` توی روتر،
که ممکنه تا ۲۰ بار در یک درخواست صدا زده بشه) دائم داره همون چند جدول کوچیک رو
SELECT/UPDATE می‌کنه:

**پیش‌شرط امنیت این الگو**: چون `idFromName(...)` همیشه یک instance ثابت برمی‌گردونه
و متدهای حساس (که این کش رو می‌خونن/می‌نویسن) هیچ `await` واقعی داخلشون ندارن (بدنه‌شون
در یک تیک جاوااسکریپت کامل اجرا می‌شه)، نگه‌داشتن state درون‌حافظه‌ای دقیقاً به همون
اندازه‌ی SQL امنه — بدون race condition، بدون نیاز به قفل جدا.

**دو سطح متفاوت برای دو نوع داده:**

1. **داده‌ای که با فرکانس CRUD ادمین تغییر می‌کنه** (مثلاً لیست مدل‌ها/کلیدها): بارگذاری
   یک‌باره در constructor، و **write-through** — یعنی بعد از هر INSERT/UPDATE SQL،
   بلافاصله همون یک ردیف رو دوباره از SQL می‌خونی (با یک هلپر ساده مثل
   `_modelById`/`_modelByName`) و جایگزینش می‌کنی توی کش. این SELECT اضافه هزینه‌ای
   نداره چون CRUD ادمین کم‌تکراره، و مزیتش اینه که منطق محاسباتی (مثل مقدار پیش‌فرض
   `default_thinking`) فقط یک‌جا (توی SQL/DB layer) تعریف می‌شه، نه دوبار (هم توی SQL
   هم توی کش).
2. **داده‌ای که با فرکانس هر درخواست ورودی تغییر می‌کنه** (مثلاً شمارنده‌های
   rpm/rpd، یا streak خطاهای پشت‌سرهم برای circuit breaker): این‌ها کاملاً درون‌حافظه‌ای
   می‌مونن و فقط **دوره‌ای** (مثلاً هر ۵ دقیقه، با یک DO Alarm طبق الگوی بالا) با SQL
   همگام می‌شن. چون خواندن‌ها (هر درخواست) خیلی بیشتر از نوشتن‌های SQL (هر ۵ دقیقه)
   هستن، این جاست که بیشترین سود به دست میاد.

**ریسک قابل‌قبول این الگو**: اگه DO بین دو تا flush کرش کنه یا evict بشه، چند دقیقه
از شمارش از دست می‌ره. برای هر چیزی که فقط یک لایه‌ی محافظتی روی یک سهمیه‌ی خارجی است
(نه یک مرز امنیتی حساس)، این کاملاً قابل‌قبوله — بدترین حالت یک ۴۲۹ یا مشابهش از
upstream است که سیستم fallback موجود مدیریتش می‌کنه.

**⚠️ گوتچای حیاتی: `.find()` در برابر `.filter()`**
`Array.prototype.filter()` همیشه یک آرایه‌ی **جدید** برمی‌گردونه (کپی)، ولی
`Array.prototype.find()` رفرنس واقعی به همون object توی آرایه‌ی اصلی می‌ده. این فرق
مستقیم روی درستی کش اثر می‌ذاره:
- برای **خواندن صرف** (مثلاً فیلترکردن مدل‌های enabled قبل از انتخاب یکی)، از
  `.filter()` استفاده کن — نتیجه یک کپی یک‌بارمصرفه، دستکاری‌ش (splice/sort/unshift)
  به کش اصلی آسیب نمی‌زنه.
- برای **نوشتن/mutation** (مثلاً ریست‌کردن `fail_streak` بعد از یک موفقیت)، از
  `.find()` استفاده کن — باید همون object توی کش اصلی mutate بشه، نه یک کپی که دور
  ریخته می‌شه. قاطی‌کردن این دو، باگی می‌سازه که چیزی throw نمی‌کنه، فقط بی‌صدا رفتار
  نادرست می‌ده (مثلاً circuit breaker هیچ‌وقت واقعاً فعال نمی‌شه).

**چک‌لیست تکمیلی الگو:**
- عملیات `delete` باید همه‌ی جاهایی که به رکورد حذف‌شده رفرنس دارن رو هم پاک کنه —
  نه فقط از کش اصلی، بلکه از هر Map کمکی دیگه هم (مثلاً شمارنده‌های ریت‌لیمیت مرتبط
  با مدل/کلید حذف‌شده)، وگرنه رکوردهای یتیم توی حافظه می‌مونن و در flush بعدی دوباره
  توی SQL می‌شینن (بی‌ضرر ولی کثیف).
- هر مسیر «کوئری خام SQL» (مثل یک `/admin/query` power-user) که مستقیم روی جدول‌های
  کش‌شده اثر می‌ذاره، باید بعد از اجرا **کل کش رو از نو از SQL بارگذاری کنه** — تلاش
  برای حدس‌زدن «کدوم جدول لمس شده» یک heuristic شکننده‌ست؛ reload کامل ساده‌تر و
  مطمئن‌تره، مخصوصاً وقتی این مسیر خودش کم‌استفاده‌ست.
- هر متد observability (مثل `getStatus()`) که قبلاً مستقیم SQL می‌خوند، باید بعد از
  این تغییر از همون کش بخونه — وگرنه ابزارهای مانیتورینگ (بات تلگرام، `/admin/status`)
  عمداً همیشه یک قدم عقب‌تر از رفتار واقعی سیستم می‌مونن.
- alarm ای که این flush رو انجام می‌ده، باید حتی روی خطا هم خودش رو دوباره
  زمان‌بندی کنه (`try { ... } catch { log } finally-مانند: همیشه setAlarm بزن`) —
  وگرنه یک خطای throw‌شده‌ی تکی، کل مکانیزم durability رو برای همیشه می‌کشه.

---

## ۳) محدودیت‌های واقعی Cloudflare و کجا با بیلینگ گیر می‌کنی

| سرویس | نیاز به کارت/بیلینگ حتی برای رایگان؟ | نکته |
|---|---|---|
| Workers (Free) | نه | ۱۰۰,۰۰۰ درخواست/روز، کافی برای اکثر پروژه‌های شخصی |
| Durable Objects (SQLite) | نه | نیاز به migration با `new_sqlite_classes` |
| Cron Triggers | نه | رایگان، ولی حداقل بازه معمولاً ۱ دقیقه‌ست |
| **R2** | **بله، تأیید شده** | حتی با مصرف صفر، فعال‌سازی کارت/PayPal می‌خواد |
| Vectorize | مستندات متناقض دیدم | خودت با `wrangler vectorize create` امتحان کن؛ ممکنه بدون کارت هم پلن رایگان واقعی داشته باشه |
| Workers Paid ($۵/ماه) | بله | همون مانع R2 رو داره |

**درس کلی**: قبل از قفل‌کردن معماری روی یک سرویس Cloudflare خاص، اگه محدودیت
بیلینگ (مثلاً نبود دسترسی به کارت بین‌المللی) داری، حتماً با یک تست کوچیک
(`wrangler <service> create`) مطمئن شو قبل از سرمایه‌گذاری زمان روی طراحی کامل.

**جایگزین رایگان برای «دیتابیس برداری» وقتی Vectorize در دسترس نیست:**
برای حجم مصرف شخصی (چند صد/هزار بردار)، ذخیره‌ی بردارها به‌صورت JSON در همون
SQLite دیتابیس DO + جستجوی شباهت کسینوسی brute-force در کد جاوااسکریپت، کاملاً در
بودجه‌ی CPU یک DO (۳۰ ثانیه) جا می‌شه — نیازی به سرویس جدا نیست.

---

## ۴) کار با APIهای هوش مصنوعی پشت لایه‌ی «سازگار با OpenAI»

خیلی از providerها (گوگل، OpenRouter، و بقیه) یک لایه‌ی «OpenAI-compatible» ارائه
می‌دن تا کد موجودت رو عوض نکنی. این لایه‌ها **مفیدن ولی ۱۰۰٪ کامل نیستن** —
خصوصاً برای قابلیت‌های جدیدتر (audio, embeddings با ابعاد سفارشی). قبل از این‌که
کل معماری رو روی این لایه بسازی، حداقل یک تست دستی از قابلیت‌های حیاتی (نه فقط چت
ساده) بزن.

### مشکلات واقعی که برخوردیم (و راه‌حل هرکدوم)

**۱. Thought signatures (Gemini 3، multi-turn tool use)**
مدل‌های Gemini 3 روی هر function call یک امضای رمزنگاری‌شده می‌ذارن که باید عیناً
در نوبت بعد برگرده — این با تست زنده تأیید شد که **الزامی** است (بدون اون، ۴۰۰ با
پیام صریح `is missing a thought_signature` می‌گیری)، نه فقط یک ویژگی اختیاری که
لایه‌ی بتا بد handle می‌کرده. ولی این امضا در یک فیلد غیراستاندارد (`extra_content`)
زندگی می‌کنه که کلاینت‌های استاندارد OpenAI (مثل خیلی از ابزارهای موجود) نمی‌شناسنش
و گم می‌کننش، و این باعث خطای ۴۰۰ بعد از اولین دور ابزار می‌شه.
**راه‌حل**: در لایه‌ی proxy خودت، قبل از فوروارد، هر tool_call بدون امضا رو با
مقدار placeholder مستند خودِ گوگل (`context_engineering_is_the_way_to_go`) پر کن —
اعتبارسنجی رو دور می‌زنه بدون افت کیفیت محسوس. اگه بعداً مستقیم به endpoint بومی
(`generateContent`) مهاجرت کردی، این مکانیزم رو باید توی خودِ لایه‌ی ترجمه‌ت هم
بازتولید کنی (چون endpoint بومی هم دقیقاً همین الزام رو داره)، نه فقط توی proxy لایه‌ی
قدیمی.

**۲. Embeddings: پارامتر `dimensions` روی لایه‌ی OpenAI-compat نامعتبره**
اگه به یک بعد ثابت (مثلاً برای یک دیتابیس برداری) نیاز داری، لایه‌ی سازگار ممکنه
این پارامتر رو نادیده بگیره و بعد پیش‌فرض مدل رو برگردونه — خرابی بی‌صدا.
**راه‌حل**: برای embeddings مستقیم به endpoint بومی provider (`embedContent`) برو،
که پارامتر `output_dimensionality` رو واقعاً رعایت می‌کنه.

**۳. TTS: خروجی PCM خام، لایه‌ی سازگار نمی‌تونه mime type رو مدیریت کنه**
مدل‌های TTS جمنای صدا رو به‌صورت PCM خام (`audio/l16`) برمی‌گردونن. لایه‌ی
OpenAI-compat (که هنوز بتاست) این mime type رو نمی‌شناسه و ۴۰۰ می‌ده.
**راه‌حل**: مستقیم به `generateContent` بومی برو (`responseModalities: ["AUDIO"]`)،
و PCM خام رو خودت با یک هدر WAV دستی (فقط ۴۴ بایت اضافه، صفر هزینه‌ی CPU) بپیچون.

**۴. عکس با لینک ریموت (نه base64) روی endpoint بومی رد می‌شه**
مستندات و نمونه‌های رسمی گوگل برای `image_url` همیشه `data:...;base64,...` نشون
می‌دن، هیچ‌وقت لینک خام (`https://...`). اگه یک ابزار (مثل جستجوی عکس) عادت داشته
باشه فقط URL رو مستقیم پاس بده، روی endpoint بومی با `400 INVALID_ARGUMENT` رد
می‌شه. **راه‌حل**: قبل از فرستادن، همیشه عکس رو دانلود و به `data:` base64 تبدیل کن؛
هیچ‌وقت فرض نکن یک endpoint چندوجهی «سازگار با OpenAI» لینک ریموت رو خودش fetch
می‌کنه، حتی اگه لایه‌ی بتای همون provider قبلاً این کار رو می‌کرده.

**۵. auth بومی با auth سازگار فرق داره**
لایه‌ی OpenAI-compat: `Authorization: Bearer <key>`.
Endpoint بومی گوگل: هدر `x-goog-api-key: <key>` (یا `?key=` در URL).
این تفاوت رو موقع سوییچ بین دو مسیر فراموش نکن.

**۶. کدهای خطا معنی متفاوتی برای استراتژی retry دارن**
- `429` → کلید تمومه، **کلید رو عوض کن**، مدل رو نه.
- `503`/`502`/`504` (overloaded) → مشکل خودِ مدله، **مدل رو عوض کن**، نه فقط کلید.
- `524` → این کد اختصاصی خودِ **Cloudflare edge**ست (نه واقعاً از upstream)، معنیش
  «origin به‌موقع جواب نداد»؛ از نظر استراتژی retry باید مثل ۵۰۲/۵۰۳/۵۰۴ رفتار کنه
  (مدل رو عوض کن)، چون معمولاً نشونه‌ی کند/ناپایدار بودن upstreamه، نه مشکل خودِ
  درخواست.
- `400` → مشکل شکل درخواسته، retry کردن با کلید/مدل دیگه فایده نداره، فوراً fail کن.
- محدودیت واقعی (real quota) روی حساب ممکنه با چیزی که خودت فرض کردی فرق کنه —
  همیشه پیام خطا رو parse کن، به رشته‌ی «day»/«daily» نگاه کن تا بفهمی محدودیت
  روزانه بوده یا دقیقه‌ای. این heuristic معمولاً روی متن خطای هم لایه‌ی بتا هم
  endpoint بومی کار می‌کنه، چون هر دو از یک زیرساخت quota مشترک گوگل خطا می‌گیرن.

**۷. محدودیت‌های ساختاری خودِ مکالمه**
بعضی APIهای مکالمه‌ای (Gemini) قاعده دارن که آرایه‌ی پیام‌ها نباید با نوبت
assistant/model تموم بشه («Requests ending with a model turn are not
supported»). اگه کلاینتی که باهاش کار می‌کنی گاهی state ناقص می‌سازه (مثلاً بعد از
یک تولید نیمه‌کاره)، این خطا رو می‌گیری — چیزی که لزوماً از سمت proxy تو قابل حل
نیست، ولی خوبه بدونی از کجا میاد.

### مهاجرت کامل از یک لایه‌ی بتا — یک روش‌شناسی که در عمل جواب داد

وقتی به اندازه‌ی کافی از مشکلات بند‌های بالا خوردیم (سه بار جدا، سه قابلیت متفاوت)،
تصمیم گرفتیم به‌جای پچ‌کردن مورد به مورد، کل مسیر چت رو هم مستقیم به endpoint بومی
مهاجرت بدیم. روش‌شناسی‌ای که استفاده شد و ارزش تکرار در پروژه‌های مشابه رو داره:

1. **لایه‌ی ترجمه رو به‌عنوان توابع خالص، جدا از هر I/O، بنویس.** دو تابع (یکی برای
   درخواست، یکی برای پاسخ)، بدون side effect، قابل تست با یک اسکریپت Node.js ساده و
   بدون نیاز به شبکه یا حتی خودِ Workers runtime. این جدایی باعث شد بشه با اطمینان
   بالا (چند ده assertion، پوشش سناریوهای چندابزاره، چندرسانه‌ای، thinking config) قبل
   از هر دیپلوی واقعی، منطق رو راست‌آزمایی کرد.
2. **مجهولات رو با تست زنده حل کن، نه با فرض از روی مستندات.** قبل از نوشتن کد نهایی،
   چندتا سؤال باز («role دقیق پاسخ تابع چیه؟»، «thought_signature واقعاً اجباریه یا
   فقط لایه‌ی بتا سخت‌گیر بوده؟»، «seed/penalty ها پشتیبانی می‌شن؟») مستقیم با `curl`
   به endpoint بومی (با یک کلید واقعی) تست شدن، نتیجه مستند شد، و فقط بعدش کد نوشته
   شد. این جلوی چندین فرض غلط رو گرفت (مثلاً معلوم شد penalty ها با ۴۰۰ رد می‌شن، نه
   این‌که فقط نادیده گرفته بشن).
3. **قبل از پروداکشن، یک worker/DO کاملاً جدا (canary) با یک snapshot واقعی از داده‌ی
   پروداکشن seed کن.** این یعنی تست نهایی نه با داده‌ی ساختگی، بلکه با همون مدل‌ها و
   کلیدهای واقعی (در یک namespace کاملاً مجزا) انجام می‌شه — بدون هیچ ریسکی برای
   ترافیک زنده.
4. **cutover به پروداکشن رو گیت‌دار کن.** حتی بعد از تست‌های موفق روی canary، دیپلوی
   نهایی روی worker واقعی فقط با تأیید صریح انسانی انجام شد، نه خودکار — و یک دستور
   رول‌بک آماده (`wrangler rollback`) از قبل شناخته‌شده بود.
5. **فازهای پرریسک‌تر رو عمداً به تعویق بنداز.** استریم (که نیاز به بازسازی کامل SSE
   بومی به شکل SSE استاندارد OpenAI داره — یک state machine واقعی، نه یک ترجمه‌ی
   ساده) و یک Files API عمومی (قابلیت جدید، نه ترجمه‌ی چیز موجود) عمداً از این فاز
   جدا نگه داشته شدن، چون ریسک/پیچیدگی‌شون بیشتر از بقیه بود و هیچ caller واقعی
   فعلاً بهشون نیاز نداشت.

این روش (توابع خالص + تست واحد → تست زنده‌ی مجهولات → canary با داده‌ی واقعی → cutover
گیت‌دار → به‌تعویق‌انداختن آگاهانه‌ی بخش‌های پرریسک) یک الگوی قابل‌استفاده‌ی مجدده
برای هر مهاجرت مشابه در آینده (مثلاً همون فاز استریم یا Files API که هنوز باز مونده).

---

## ۵) الگوهای معماری برای «روتر چندکلید/چندمدل» جلوی یک API محدود

اگه با چند کلید API رایگان یا محدود کار می‌کنی، این الگوها خیلی ارزش عملی دارن:

- **مدیریت کلید رو یک‌جا متمرکز کن.** یک سرویس (Worker) مسئول همه‌ی کلیدها/مدل‌ها
  باشه؛ بقیه‌ی سرویس‌ها فقط از طریق یک endpoint داخلی به اون وصل بشن، بدون این‌که
  خودشون کلید نگه دارن. اضافه‌کردن سرویس جدید یا سرویس‌های آینده خیلی راحت‌تر می‌شه.
- **تگ‌گذاری «kind»/«capability»** (مثلاً chat/tts/embedding) روی هر مدل، تا
  زنجیره‌ی fallback هیچ‌وقت بین قابلیت‌های ناهم‌جنس رد نشه (یک درخواست TTS نباید
  به یک مدل متنی برسه).
- **انتخاب کلید least-recently-used** برای پخش بار یکنواخت بین کلیدها.
- **مدار قطع خودکار (circuit breaker)**: بعد از N بار خطای پشت‌سرهم از یک مدل،
  اون مدل رو چند ثانیه/دقیقه کاملاً از چرخه‌ی انتخاب کنار بذار — از رفت‌وبرگشت
  تکراری و کند به یک backend که مشخصاً خرابه جلوگیری می‌کنه. اگه این فیلدها رو
  درون‌حافظه‌ای/کش کردی (بخش ۲)، اثرش باید همچنان **بلافاصله** روی درخواست بعدی
  اعمال بشه، حتی اگه نوشتن durable‌ش (برای بقای بعد از crash) به‌صورت batch/دوره‌ای
  انجام بشه.
- **provider abstraction با یک فیلد ساده + نگاشت endpoint**: اضافه‌کردن provider
  دوم (مثلاً OpenRouter کنار Google) فقط یک ستون جدید و یک if/else کوچیکه، نه
  بازنویسی کل سیستم. یک provider که خودش از اول یک API پایدار (نه بتا) سازگار با
  OpenAI داره، نیازی به لایه‌ی ترجمه‌ی اختصاصی نداره — مهاجرت به endpoint بومی فقط
  برای providerهایی لازمه که واقعاً پشت یک لایه‌ی بتا/ناقص هستن.
- **timeout صریح روی هر fetch به سرویس بیرونی، همیشه.** بدون
  `AbortSignal.timeout(...)`، یک سرویس بیرونیِ گیرکرده می‌تونه کل زنجیره رو برای
  همیشه معلق نگه داره — این دقیقاً یکی از باگ‌های واقعی این پروژه بود.
- **کدهای وضعیت خاص خودِ لایه‌ی شبکه (نه فقط upstream) رو هم لحاظ کن.** مثلاً `524`
  کد اختصاصی Cloudflare edge برای «origin جواب نداد» است، نه یک کد واقعی از upstream
  — ولی از نظر استراتژی retry باید مثل خطای مدل‌محور (۵۰۲/۵۰۳/۵۰۴) رفتار کنه.

---

## ۶) تلگرام به‌عنوان رابط مدیریتی (بدون داشبورد وب)

برای پروژه‌های شخصی، یک بات تلگرام با چند دستور متنی + دکمه‌ی اینلاین، **کاملاً
جایگزین یک پنل ادمین وب** می‌شه — صفر هزینه‌ی توسعه‌ی frontend:

- دکمه‌های اینلاین (`inline_keyboard` + `callback_query`) برای عملیات تعاملی
  (مثلاً تغییر اولویت با ⬆️/⬇️) بدون نیاز به تایپ دستور.
- یک allowlist ساکت بر پایه‌ی `chat_id` (بدون لاگین واقعی) کافیه برای امنیت پایه‌ی
  یک بات شخصی.
- برای پیام‌های طولانی، تلگرام سقف ۴۰۹۶ کاراکتر داره — باید chunk کنی؛ اگه از
  `parse_mode: HTML` استفاده می‌کنی، **splitter باید از تگ‌های باز/بسته آگاه باشه**
  وگرنه ممکنه وسط یک `<pre><code>` قیچی کنه و کل پیام fail بشه.
- تبدیل Markdown مدل به HTML تلگرام: اول escape کن (`&`, `<`, `>`)، بعد بلاک‌کد و
  کد inline رو با placeholder موقت جدا کن (تا محتوای کدشون توسط regexهای
  bold/italic بعدی خراب نشه)، بعد بقیه‌ی markdown رو تبدیل کن، آخر placeholderها
  رو برگردون.
- `<blockquote expandable>` تگ رسمی تلگرام برای متن آکاردیونی/جمع‌شونده‌ست.
- هر پیام مدیریتی که یک وضعیت رو بعد از تغییر دوباره نشون می‌ده (مثل `/priority` یا
  `/status`)، باید از همون منبع تازه‌ی داده بخونه که خودِ سیستم موقع تصمیم‌گیری واقعی
  ازش استفاده می‌کنه (کش درون‌حافظه‌ای، نه SQL که ممکنه چند دقیقه عقب‌تر باشه) — وگرنه
  ابزار مدیریتی خودش منبع سردرگمی می‌شه.

---

## ۷) گوتچاهای اجرایی JavaScript مخصوص محیط Workers

- **`String.fromCharCode(...bigTypedArray)` روی آرایه‌های بزرگ crash می‌کنه.**
  اسپرد کردن یک آرایه‌ی بزرگ در آرگومان‌های یک تابع، به محدودیت تعداد آرگومان
  موتور JS می‌خوره (حدود ۱۲۵-۱۳۰ هزار آرگومان در V8). برای تبدیل byte array به
  base64، **همیشه chunk‌به‌chunk** (مثلاً هر ۸ کیلوبایت) پردازش کن:
```js
  function bytesToBase64(bytes) {
    let binary = "";
    for (let i = 0; i < bytes.length; i += 8192) {
      binary += String.fromCharCode(...bytes.subarray(i, i + 8192));
    }
    return btoa(binary);
  }
```
  این باگ خیلی موذیه چون فایل‌های کوچیک (زیر ~۱۳۰KB) کاملاً درست کار می‌کنن و فقط
  با فایل‌های واقعی (عکس/فایل واقعی که معمولاً بزرگ‌تره) خودش رو نشون می‌ده.
- `btoa`/`atob` مستقیم در Workers در دسترسن (مثل مرورگر)، نیازی به `Buffer`
  (مخصوص Node) نیست.
- `FormData` + `Blob` برای آپلود multipart (مثل ارسال عکس/صدا به تلگرام) به‌صورت
  بومی در `fetch` کار می‌کنه.
- ساخت WAV از PCM خام فقط یک هدر ۴۴ بایتی ثابته (RIFF/WAVE/fmt/data chunks) —
  هیچ نیازی به کتابخانه‌ی صوتی یا CPU اضافه نداره.
- **کش‌کردن یک تبدیل خالص (pure transformation) روی داده‌ی تکراری بین درخواست‌ها،
  یک `Map` سطح-ماژول ساده کافیه.** اگه یک caller همون آرایه/آبجکت رو (مثلاً لیست
  تعریف ابزارهای یک ایجنت) بین درخواست‌های پشت‌سرهم عیناً تکرار می‌کنه، یک کش با کلید
  `JSON.stringify(input)` و یک سقف اندازه‌ی ساده (وقتی پر شد، کامل `clear()` کن —
  نیازی به LRU واقعی نیست مگر تعداد caller های متفاوت واقعاً زیاد باشه) هزینه‌ی
  محاسبه‌ی تکراری رو حذف می‌کنه، با این پیش‌فرض که خودِ `JSON.stringify` روی ورودی
  معمولاً ارزون‌تر از تبدیلی‌یه که داره جایگزینش می‌کنه (مخصوصاً وقتی اون تبدیل خودش
  object های جدید می‌سازه، مثل یک بازنویسی بازگشتی schema). این کش، برخلاف کشی که
  داخل یک Durable Object زندگی می‌کنه، مستقیم روی بودجه‌ی CPU خودِ Worker اثر می‌ذاره
  (بخش ۱).

---

## ۸) روش‌شناسی تست بدون دسترسی شبکه‌ی زنده

- منطق دیتابیس (Durable Object) رو با ماژول `node:sqlite` (تجربی ولی کاربردی)
  به‌جای شبیه‌سازی دستی تست کن — واقعاً SQL اجرا می‌شه، نه یک mock ساختگی.
- برای شبیه‌سازی سرویس‌های بیرونی، `global.fetch` رو mock کن و بر اساس URL
  route کن؛ ولی حواست باشه mock باید **همه‌ی pattern هایی که کد واقعی می‌زنه** رو
  پوشش بده (مثلاً هم `getFile` و هم دانلود فایل تلگرام، نه فقط `sendMessage`) —
  یک mock ناقص می‌تونه یک خطا رو قایم کنه و یک نتیجه‌ی گمراه‌کننده بده.
- برای باگ‌های race condition، تست synchronous ساده کافی نیست — باید عمداً
  تأخیر (`setTimeout`) به متدهای stub اضافه کنی تا رفتار RPC واقعی رو شبیه‌سازی
  کنی و ببینی ترتیب واقعی اجرا چیه.
- برای پیدا کردن آستانه‌ی دقیق یک باگ اندازه‌محور (مثل حد بحرانی
  stack overflow)، باینری‌سرچ روی اندازه‌ی ورودی بهترین راهه — تک‌تک اندازه‌ها رو
  تست کن تا نقطه‌ی دقیق گذر پیدا بشه.
- همیشه صادقانه فرق بگذار بین «این منطق واقعاً با اجرای واقعی تست شد» و «این فقط
  بر اساس مستندات نوشته شد و نیاز به تأیید زنده داره» — خصوصاً برای APIهای
  بیرونی‌ای که نمی‌شه بدون کلید واقعی تستشون کرد.

### اجرای کد یک Durable Object واقعی هم توی Workers هم توی Node، بدون نگه‌داشتن دو نسخه‌ی جدا

اگه می‌خوای همون فایل production کلاس DO رو (نه یک کپی جدا) هم روی Workers واقعی هم
توی یک اسکریپت تست Node.js (با `node:sqlite`) اجرا کنی، یک ترفند عملیه: کلاس پایه رو
با یک dynamic import محافظت‌شده بگیر، و برای محیط Node یک fallback ساده تعریف کن:

```js
let DurableObjectBase;
try {
  const workers = await import("cloudflare:workers");
  DurableObjectBase = workers.DurableObject;
} catch {
  DurableObjectBase = class {
    constructor(ctx, env) { this.ctx = ctx; this.env = env; }
  };
}
```

و در `package.json`، فیلد `imports` (subpath imports استاندارد Node.js) رو برای
remap کردن `cloudflare:workers` به یک فایل mock محلی موقع اجرای تست تنظیم کن:

```json
{
  "imports": {
    "cloudflare:workers": "./tests/mockCloudflareWorkers.js"
  }
}
```

روی خودِ Workers runtime، `cloudflare:workers` یک ماژول native داخلیه که همیشه باید
موفق resolve بشه، پس مسیر `catch` هیچ‌وقت گرفته نمی‌شه — این ترفند فقط برای محیط
Node.js (تست) فعال می‌شه. **این الگو کار می‌کنه، ولی یک نکته‌ی آگاهی داره:** یک مسیر
«اگه import بومی شکست خورد، یک کلاس جایگزین ساختگی بساز» حالا توی خودِ فایل
production زندگی می‌کنه. برای اطمینان بیشتر در پروژه‌های آینده، جایگزین قوی‌تری هست:
ابزار رسمی کلادفلر `@cloudflare/vitest-pool-workers` که تست‌ها رو داخل یک محیط
Workers **واقعی** (workerd، نه یک شبیه‌سازی دستی با `node:sqlite`) اجرا می‌کنه — هم
این ترفند dual-import رو کاملاً زائد می‌کنه، هم اطمینان بیشتری می‌ده که رفتار DO
(از جمله semantics دقیق storage API) دقیقاً همونیه که روی پروداکشن اتفاق می‌افته.

---

## ۹) چک‌لیست برای شروع پروژه‌ی بعدی

- [ ] آیا این پروژه به CPU زیاد نیاز داره؟ منطق سنگین رو از اول توی یک Durable
      Object بذار، نه مستقیم توی fetch handler.
- [ ] آیا از سرویسی مثل R2/Vectorize/Workers Paid استفاده می‌کنی؟ قبل از طراحی
      کامل، یک بار با `wrangler <service> create` چک کن که بیلینگ نمی‌خواد.
- [ ] آیا با یک لایه‌ی «سازگار با OpenAI» کار می‌کنی؟ قابلیت‌های غیرمتنی (صدا،
      embedding با بعد دلخواه، حافظه‌ی چندمرحله‌ای ابزار، چندرسانه‌ای) رو قبل از تعهد
      کامل به این لایه، جدا و با تست زنده (نه فقط مستندات) بررسی کن. اگه بارها به
      محدودیت‌های تکراری همون لایه خوردی، مهاجرت کامل به endpoint بومی رو با روش‌شناسی
      فازبندی‌شده (بخش ۴) در نظر بگیر، نه پچ‌کردن مورد به مورد.
- [ ] هر `fetch` به یک سرویس بیرونی، `AbortSignal.timeout(...)` داره؟
- [ ] اگه چند کلید/چند مدل داری، مکانیزم انتخاب رو از اول با در نظر گرفتن
      «۴۲۹ یعنی عوض کن کلید، ۵۰۳/۵۲۴ یعنی عوض کن مدل» طراحی کن.
- [ ] هر `stub.method()` که صدا می‌زنی، `await` داره؟
- [ ] برای تبدیل byte array بزرگ به base64، از نسخه‌ی chunk‌شده استفاده می‌کنی؟
- [ ] منطق DB رو با `node:sqlite` واقعی تست کردی، نه فقط با چشم خوندی؟
- [ ] آیا یک متد پرتکرار داخل یک DO تک‌نویسنده دائم داره همون چند جدول کوچیک رو
      SELECT می‌کنه؟ کش درون‌حافظه‌ای با write-through برای CRUD کم‌تکرار و
      batch-flush (با DO Alarm، نه `setInterval`) برای شمارنده‌های پرتکرار رو در نظر
      بگیر — و مطمئن شو هر مسیر observability و هر «کوئری خام SQL» با همین کش سازگاره.
- [ ] اگه هدفت کم‌کردن CPU-time خودِ **Worker** (نه DO) است، مطمئن شو داری روی کدی
      کار می‌کنی که واقعاً مستقیم داخل Worker اجرا می‌شه، نه کدی که فقط از طریق یک DO
      stub صدا زده می‌شه — این دو بودجه‌ی کاملاً جدا هستن.
- [ ] برای تست منطق DO، آیا محیط اجراییت (mock دستی در برابر
      `@cloudflare/vitest-pool-workers`) به‌اندازه‌ی کافی به رفتار واقعی Workers
      runtime نزدیکه؟